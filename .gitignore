node_modules/
dist/
*.local
# *.local výše nechytá tohle jméno (končí na .md, ne na .local) — osobní branch
# prefix pro Git workflow, viz CLAUDE.md. Každý má svůj, negitovaný.
CLAUDE.local.md
# *.local výše nepokrývá tohle jméno (končí na .json, ne na .local) — osobní
# oprávnění Claude Code se nesmí dostat do veřejného repa.
.claude/settings.local.json
.vite/
Thumbs.db
.DS_Store
.screenshots/
.regrese.json

# Grafické podklady (mockupy, listy kreseb a ikon) do repa nepatří —
# jsou to zdrojáky návrhu, ne aplikace. Viz CLAUDE.md.
grafika/
